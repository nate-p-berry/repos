<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectTwo.Views.NewsPage"
             Title="News">
    <Grid>
        <!--<VerticalStackLayout>
            <Label 
            Text="Total results found: "
            VerticalOptions="Center" 
            HorizontalOptions="Center" FontAutoScalingEnabled="True" FlowDirection="LeftToRight" />
            <Label 
            Text="{Binding Source=NewsPage.resultCount}"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            FontAutoScalingEnabled="True"
            FlowDirection="LeftToRight" />
        </VerticalStackLayout>-->
        <ListView Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout>
                        <Image Source="{Binding UrlToImage}" VerticalOptions="Fill" HorizontalOptions="Fill" />
                        <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="Header" />
                        <Label Text="{Binding Description}" FontAttributes="Italic" FontSize="Body" />
                    </VerticalStackLayout>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>